export type CountryOption = {
    key: string;
    label: string;
    value: string;
}
export type NotNormalizedFormValue = {
    lastName: string,
    firstName: string,
    secondName: string,
    address: string,
    homeNumber: string,
    mobileNumber: string,
    prefix?: string
}