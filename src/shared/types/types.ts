export type Record = {
    lastName: string,
    firstName: string,
    secondName: string,
    address: string,
    homeNumber: string,
    mobileNumber: string
}
export enum recordFields {
    lastName = "lastName",
    firstName = "firstName",
    secondName = "secondName",
    address = "address",
    homeNumber = "homeNumber",
    mobileNumber = "mobileNumber"
}